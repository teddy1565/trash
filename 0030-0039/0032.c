#include<stdio.h>
#include<stdlib.h>
int main(){
  int _N=10000;
  char *str=(char*)malloc(sizeof(char)*N);
  while(1){
    int start=0,end=0;
    scanf("%s",str);
    scanf("%d%d",&start,&end);
  }
}